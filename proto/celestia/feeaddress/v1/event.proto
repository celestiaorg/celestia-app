syntax = "proto3";
package celestia.feeaddress.v1;

import "cosmos_proto/cosmos.proto";

option go_package = "github.com/celestiaorg/celestia-app/x/feeaddress/types";

// EventFeeForwarded is emitted when tokens are forwarded to the fee collector.
message EventFeeForwarded {
  // from is the address from which tokens were forwarded (the fee address)
  string from = 1 [(cosmos_proto.scalar) = "cosmos.AddressString"];
  // amount is the amount of tokens forwarded (e.g., "1000000utia")
  string amount = 2;
}
