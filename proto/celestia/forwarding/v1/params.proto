syntax = "proto3";
package celestia.forwarding.v1;

option go_package = "github.com/celestiaorg/celestia-app/x/forwarding/types";

// Params defines the parameters for the forwarding module.
// Currently empty; reserved for future governance-controlled parameters.
message Params {
  // Note: TIA collateral token is derived from warp module at runtime
  // by iterating tokens with OriginDenom="utia" and checking enrolled routers.
}
